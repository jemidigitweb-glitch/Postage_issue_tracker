import {
  BookOpen,
  FolderOpen,
  Wrench,
  Inbox,
  Scale,
  FileCheck,
  ShieldCheck,
  Handshake,
  type LucideIcon,
} from "lucide-react";

export const iconMap: Record<string, LucideIcon> = {
  BookOpen,
  FolderOpen,
  Wrench,
  Inbox,
  Scale,
  FileCheck,
  ShieldCheck,
  Handshake,
};
